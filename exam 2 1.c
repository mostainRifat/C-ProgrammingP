#include<stdio.h>
int main()
{
    int k=0,i,t;
    long long int s,a,b,c;

    scanf("%d",&t);
    if(0<t<101&& 0<=s,a,b,c<= 2^32)
    {
    for(i=1;i<=t;i++)
    {
    scanf("%lld %lld %lld %lld ",&s,&a,&b,&c);

    k= s - ( a + b + c) ;

    printf("%d \n ", k );
    }
    }
    return 0;
}
